name: solver-codex

on:
  workflow_dispatch:
    inputs:
      issue_number:
        description: "Issue number to solve"
        required: true

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Skeleton
        run: echo "Skeleton workflow. Run from feature branch for full logic."
